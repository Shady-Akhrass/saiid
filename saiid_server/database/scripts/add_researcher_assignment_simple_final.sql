-- =====================================================
-- SQL Script: تحديث enum status فقط
-- تاريخ الإنشاء: 2025-12-22
-- الوصف: تحديث enum status فقط (assigned_researcher_id موجود مسبقاً)
-- =====================================================
-- 
-- ⚠️ مهم: قم بعمل backup لقاعدة البيانات قبل التنفيذ!
-- =====================================================

-- =====================================================
-- 1. تحديث enum status - إضافة الحالات الجديدة
-- =====================================================

-- أولاً: إضافة "مسند لباحث" و "تم التنفيذ" إلى ENUM
ALTER TABLE `project_proposals` MODIFY COLUMN `status` ENUM(
    'جديد',
    'قيد التوريد',
    'تم التوريد',
    'قيد التوزيع',
    'مسند لباحث',
    'جاهز للتنفيذ',
    'تم اختيار المخيم',
    'قيد التنفيذ',
    'منفذ',
    'تم التنفيذ',
    'في المونتاج',
    'تم المونتاج',
    'وصل للمتبرع',
    'ملغى',
    'يجب إعادة المونتاج',
    'مؤجل'
) NOT NULL DEFAULT 'جديد';

-- =====================================================
-- 2. تحديث المشاريع من "منفذ" إلى "تم التنفيذ"
-- =====================================================

UPDATE `project_proposals` 
SET `status` = 'تم التنفيذ' 
WHERE `status` = 'منفذ';

-- =====================================================
-- 3. إزالة "منفذ" و "قيد التوزيع" من ENUM
-- =====================================================

ALTER TABLE `project_proposals` MODIFY COLUMN `status` ENUM(
    'جديد',
    'قيد التوريد',
    'تم التوريد',
    'مسند لباحث',
    'جاهز للتنفيذ',
    'تم اختيار المخيم',
    'قيد التنفيذ',
    'تم التنفيذ',
    'في المونتاج',
    'تم المونتاج',
    'وصل للمتبرع',
    'ملغى',
    'يجب إعادة المونتاج',
    'مؤجل'
) NOT NULL DEFAULT 'جديد';

-- =====================================================
-- 4. Foreign Key (اختياري - يمكن تخطيه إذا ظهر خطأ)
-- =====================================================

-- إذا ظهر خطأ "Duplicate key" في الخطوة التالية، يمكنك تخطيها
-- Foreign Key موجود مسبقاً أو يمكن إضافته لاحقاً

-- ALTER TABLE `project_proposals` 
-- ADD CONSTRAINT `project_proposals_assigned_researcher_id_foreign` 
-- FOREIGN KEY (`assigned_researcher_id`) 
-- REFERENCES `team_personnel` (`id`) 
-- ON DELETE SET NULL;

-- =====================================================
-- ✅ تم الانتهاء!
-- =====================================================
-- 
-- ملخص التغييرات:
-- 1. ✅ تم تحديث enum status لإضافة "مسند لباحث" و "تم التنفيذ"
-- 2. ✅ تم تحديث جميع المشاريع من "منفذ" إلى "تم التنفيذ"
-- 3. ✅ تم إزالة "منفذ" و "قيد التوزيع" من enum
-- 4. ⚠️ Foreign Key: تم تعطيله (يمكن إضافته لاحقاً إذا لزم الأمر)
-- 
-- =====================================================

-- ============================================
-- إضافة عمود is_urgent (عاجل) - نسخة بسيطة
-- ============================================
-- التاريخ: 7 يناير 2026
-- نسخة بسيطة بدون تحقق (إذا كان العمود موجوداً سيظهر خطأ يمكن تجاهله)
-- ============================================

-- إضافة العمود is_urgent بعد status
ALTER TABLE `project_proposals` 
ADD COLUMN `is_urgent` TINYINT(1) NOT NULL DEFAULT 0 COMMENT 'مشروع عاجل' 
AFTER `status`;

-- إضافة Index للبحث السريع
CREATE INDEX `project_proposals_is_urgent_index` 
ON `project_proposals` (`is_urgent`);

-- ============================================
-- ✅ تم إضافة عمود is_urgent بنجاح
-- ============================================

